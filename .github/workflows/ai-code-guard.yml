name: AI Code Guard

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run AI Code Guard
        uses: salehhashemi1992/ai-code-guard@<commit-sha>
        with:
          path: .
